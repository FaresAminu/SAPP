% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sapp.R
\name{plot_sapp}
\alias{plot_sapp}
\title{SAPP: Professional Enhanced Visualization}
\usage{
plot_sapp(data, importances, influence, alpha = 0.6, scale_coords = TRUE, ...)
}
\arguments{
\item{data}{Data frame of original features (numeric).}

\item{importances}{Named numeric vector of feature importances.}

\item{influence}{Vector of length nrow(data) indicating the MOST influential feature.}

\item{alpha}{Adjustment strength. Numeric (0-1) OR "auto".}

\item{scale_coords}{Whether to scale PCA coordinates to unit disk (default TRUE).}

\item{...}{Additional arguments passed to \code{ggplot2::geom_point}.}
}
\value{
A \code{ggplot} object.
}
\description{
SAPP: Professional Enhanced Visualization
}
\examples{
\donttest{
library(sappviz)
data(iris)
X <- iris[, 1:4]
model <- lm(Petal.Width ~ Sepal.Length + Sepal.Width + Petal.Length, data = iris)
imp <- abs(coef(model)[-1])
names(imp) <- c("Sepal.Length", "Sepal.Width", "Petal.Length")
inf <- influence_feature(X, model)
plot_sapp(X, imp, inf, alpha = "auto")
}
}
